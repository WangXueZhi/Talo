Use the installed Project Memory Skill for substantial work in registered local projects. Run the
bundled CLI only; never parse or edit Project Memory files directly. Detect the current project,
recall task-relevant candidates, deep-read only recommended memories within the default budget,
and create a shared proposal for durable work. Use `story` for one project and `open` for the global
Memory Hub. Do not read another project without an explicit one-way read-only link.
