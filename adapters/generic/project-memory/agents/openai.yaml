interface:
  display_name: "Talo"
  short_description: "Never start over when you switch AI agents"
  default_prompt: "Use $project-memory to recall only task-relevant reviewed memory within budget. Preserve evidence-backed work as reviewed event chains such as observation to finding to cause or result, then explain why, action, evidence, outputs, conclusions, and next steps through the shared review inbox."
policy:
  allow_implicit_invocation: true
